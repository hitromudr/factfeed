---
phase: 04-web-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - factfeed/web/main.py
  - factfeed/web/routes/__init__.py
  - factfeed/web/routes/search.py
  - factfeed/web/routes/article.py
  - factfeed/web/deps.py
  - factfeed/templates/base.html
  - factfeed/static/style.css
autonomous: true
requirements:
  - INFRA-02
  - INFRA-04
  - UI-06

must_haves:
  truths:
    - "FastAPI serves HTML pages at / and /article/{id} via Jinja2 templates"
    - "Static CSS is served at /static/style.css"
    - "No login, accounts, or session cookies exist anywhere in the application"
    - "Base template includes viewport meta tag and mobile-first CSS"
  artifacts:
    - path: "factfeed/web/deps.py"
      provides: "Async DB session dependency for route injection"
      exports: ["get_db"]
    - path: "factfeed/web/routes/search.py"
      provides: "Search page route stub"
      exports: ["router"]
    - path: "factfeed/web/routes/article.py"
      provides: "Article detail route stub"
      exports: ["router"]
    - path: "factfeed/templates/base.html"
      provides: "Shared HTML layout with HTMX CDN, viewport meta, nav"
      contains: "htmx.org"
    - path: "factfeed/static/style.css"
      provides: "Sentence highlighting classes, tooltip, mobile-responsive styles"
      contains: ".fact"
  key_links:
    - from: "factfeed/web/main.py"
      to: "factfeed/web/routes/search.py"
      via: "app.include_router(search.router)"
      pattern: "include_router.*search"
    - from: "factfeed/web/main.py"
      to: "factfeed/web/routes/article.py"
      via: "app.include_router(article.router)"
      pattern: "include_router.*article"
    - from: "factfeed/web/main.py"
      to: "factfeed/static/"
      via: "app.mount('/static', StaticFiles(...))"
      pattern: "mount.*static"
---

<objective>
Set up the FastAPI web infrastructure: Jinja2 templates, static file serving, route modules, DB session dependency, base HTML template with HTMX CDN, and the complete CSS stylesheet including sentence highlighting, tooltips, and mobile-responsive layout.

Purpose: Establish the web layer foundation that all subsequent plans (search page, article detail) build upon.
Output: Working FastAPI app serving a base HTML page at `/` with static CSS, HTMX loaded, and route stubs for search and article detail.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@factfeed/web/main.py
@factfeed/db/session.py
@factfeed/db/models.py
@factfeed/config.py
@.planning/phases/04-web-interface/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create web infrastructure files (deps, routes, templates, static CSS)</name>
  <files>
    factfeed/web/deps.py
    factfeed/web/routes/__init__.py
    factfeed/web/routes/search.py
    factfeed/web/routes/article.py
    factfeed/templates/base.html
    factfeed/static/style.css
  </files>
  <action>
    Create `factfeed/web/deps.py` with an async generator `get_db()` that yields an `AsyncSession` from `AsyncSessionLocal`. Pattern:
    ```python
    from typing import AsyncGenerator
    from sqlalchemy.ext.asyncio import AsyncSession
    from factfeed.db.session import AsyncSessionLocal

    async def get_db() -> AsyncGenerator[AsyncSession, None]:
        async with AsyncSessionLocal() as session:
            yield session
    ```

    Create `factfeed/web/routes/__init__.py` (empty).

    Create `factfeed/web/routes/search.py` with an `APIRouter`. Two route stubs:
    - `GET /` — renders `search.html` template with empty results list, sources list (queried from Source table with `selectinload`), and empty query. Returns full page.
    - `GET /search` — same logic but checks `request.headers.get("HX-Request")` to return `_results.html` partial for HTMX requests, or full `search.html` for non-HTMX. Accept query params: `q` (str, default ""), `source` (int|None), `from_` (str|None, alias "from"), `sort` (str, default "facts"). For now, just query all articles ordered by `published_at` DESC with `selectinload(Article.source)`, limit 50. The full search logic comes in Plan 02.
    - Import templates from a module-level `Jinja2Templates` instance pointing to `factfeed/templates`.
    - No auth, no session cookies, no user data collection anywhere (INFRA-04).

    Create `factfeed/web/routes/article.py` with an `APIRouter`:
    - `GET /article/{article_id}` — stub that queries Article by id with `selectinload(Article.source)` and `selectinload(Article.sentences)`, returns `article.html` template. Return 404 if not found. For now, pass article and empty sentence lists to template. Full highlighting logic comes in Plan 03.

    Create `factfeed/templates/base.html`:
    - HTML5 doctype, `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
    - `<title>{% block title %}FactFeed{% endblock %}</title>`
    - Link to `/static/style.css`
    - HTMX CDN: `<script src="https://cdn.jsdelivr.net/npm/htmx.org@2.0.8/dist/htmx.min.js"></script>`
    - Simple header with "FactFeed" link to `/`
    - `{% block content %}{% endblock %}` for page content
    - No analytics, no tracking scripts, no cookies (INFRA-04)

    Create `factfeed/static/style.css` with the COMPLETE stylesheet for all of Phase 4:
    - Mobile-first base: `* { box-sizing: border-box; }`, `body { max-width: 800px; margin: 0 auto; padding: 0 1rem; font-family: system-ui, sans-serif; }`
    - Sentence highlighting classes:
      - `.fact { background-color: rgba(76, 175, 80, 0.25); }` (green)
      - `.opinion { background-color: rgba(244, 67, 54, 0.25); }` (red)
      - `.mixed { background-color: rgba(255, 193, 7, 0.25); }` (amber)
      - `.unclear { background-color: rgba(255, 193, 7, 0.15); }` (pale amber)
    - `.sentence { border-radius: 3px; padding: 0 2px; cursor: default; position: relative; display: inline; }`
    - CSS tooltip via `::after` pseudo-element on `.sentence[title]:hover::after` (position absolute, bottom 100%, dark background, white text, pointer-events none, z-index 100)
    - Opinion section styles: `.opinion-section summary { cursor: pointer; color: #666; }`, `.opinion-content { margin-top: 0.5rem; }`
    - Search form styles: input full-width, filter row with flexbox wrap, sort toggle buttons
    - Article list styles: `.article-card { border-bottom: 1px solid #eee; padding: 1rem 0; }`, headline as link, source/date in muted text, excerpt truncated
    - Responsive: no fixed widths, `word-wrap: break-word` on `.article-body`, `overflow-x: auto` on pre/code
    - Active sort button highlighted state: `.sort-btn.active { background: #1a73e8; color: white; }`
  </action>
  <verify>
    <automated>cd /home/dms/work/claude_data_parser && python -c "from factfeed.web.deps import get_db; from factfeed.web.routes.search import router as sr; from factfeed.web.routes.article import router as ar; print('imports ok')" && test -f factfeed/templates/base.html && test -f factfeed/static/style.css && echo "all files exist"</automated>
  </verify>
  <done>All web infrastructure files exist and are importable. Base template has HTMX CDN and viewport meta. CSS has all highlighting classes and mobile-responsive rules.</done>
</task>

<task type="auto">
  <name>Task 2: Wire routes and static files into FastAPI app</name>
  <files>factfeed/web/main.py</files>
  <action>
    Update `factfeed/web/main.py` to:

    1. Add imports: `from fastapi.staticfiles import StaticFiles` and `from pathlib import Path`
    2. Add `from factfeed.web.routes import search as search_routes, article as article_routes` (import AFTER app creation or inside the file)
    3. After `app = FastAPI(...)`, add:
       - `app.mount("/static", StaticFiles(directory=str(Path(__file__).resolve().parent.parent / "static")), name="static")`
       - `app.include_router(search_routes.router)`
       - `app.include_router(article_routes.router)`
    4. Keep ALL existing lifespan code unchanged (scheduler, NLP, health endpoint).
    5. Do NOT add any auth middleware, session middleware, or cookie-based state. URL query params are the only state mechanism (INFRA-04).
  </action>
  <verify>
    <automated>cd /home/dms/work/claude_data_parser && python -c "from factfeed.web.main import app; routes = [r.path for r in app.routes]; assert '/health' in routes; assert '/static' in [r.path for r in app.routes if hasattr(r, 'path') and r.path == '/static'] or any('/static' in str(r.path) for r in app.routes); print('routes:', routes)"</automated>
  </verify>
  <done>FastAPI app mounts static files at /static, includes search and article routers. Health endpoint still works. No auth or session middleware added.</done>
</task>

</tasks>

<verification>
1. `python -c "from factfeed.web.main import app"` succeeds without error
2. `/health` endpoint still accessible
3. Static files served at `/static/style.css`
4. Route stubs exist at `/`, `/search`, `/article/{article_id}`
5. No login, auth, or session cookies in any file
6. `base.html` has viewport meta tag and HTMX CDN script
</verification>

<success_criteria>
- FastAPI serves Jinja2 templates at `/` and `/article/{id}`
- Static CSS served at `/static/style.css` with all highlighting classes
- Base template loads HTMX from CDN and has mobile viewport meta
- DB session dependency injectable via `Depends(get_db)`
- No authentication, cookies, or user data collection anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-interface/04-01-SUMMARY.md`
</output>
