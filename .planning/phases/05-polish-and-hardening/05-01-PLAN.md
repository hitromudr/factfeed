---
phase: 05-polish-and-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - factfeed/web/main.py
  - factfeed/web/routes/search.py
  - tests/test_rate_limit.py
  - tests/nlp/test_pipeline.py
  - pyproject.toml
  - reports/.gitkeep
autonomous: true
requirements:
  - INFRA-05

must_haves:
  truths:
    - "Rate-limited search endpoint returns HTTP 429 after 30 requests/minute from a single IP"
    - "Accuracy test saves a JSON report artifact to reports/accuracy_report.json with overall accuracy, per-category, and per-label breakdowns"
    - "Accuracy test still fails if overall accuracy drops below 80% threshold"
    - "Default pytest run excludes slow and uat tests automatically"
  artifacts:
    - path: "factfeed/web/main.py"
      provides: "slowapi Limiter singleton and RateLimitExceeded exception handler"
      contains: "limiter"
    - path: "factfeed/web/routes/search.py"
      provides: "30/minute rate limit on search_page and search_endpoint"
      contains: "limiter.limit"
    - path: "tests/test_rate_limit.py"
      provides: "Rate limit behavior tests (429 on excess, normal under limit)"
      contains: "test_rate_limit"
    - path: "tests/nlp/test_pipeline.py"
      provides: "Accuracy report artifact saved to reports/accuracy_report.json"
      contains: "accuracy_report.json"
    - path: "reports/.gitkeep"
      provides: "Output directory for report artifacts"
  key_links:
    - from: "factfeed/web/routes/search.py"
      to: "factfeed/web/main.py"
      via: "limiter import"
      pattern: "from factfeed\\.web\\.main import limiter"
    - from: "factfeed/web/main.py"
      to: "slowapi"
      via: "Limiter instantiation and exception handler"
      pattern: "Limiter\\(key_func=get_remote_address\\)"
    - from: "tests/nlp/test_pipeline.py"
      to: "reports/accuracy_report.json"
      via: "write_text in test_evaluation_set_accuracy"
      pattern: "accuracy_report\\.json"
---

<objective>
Add per-IP rate limiting to the search endpoint and extend the classifier accuracy test to produce a reviewable JSON report artifact.

Purpose: Harden the search endpoint against abuse (30 req/min per IP, HTTP 429 on excess) and make accuracy test results machine-readable and reviewable after CI runs.

Output: Rate-limited search endpoint with test coverage; accuracy report saved to `reports/accuracy_report.json`; `pyproject.toml` updated to exclude slow/uat tests by default.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@factfeed/web/main.py
@factfeed/web/routes/search.py
@tests/nlp/test_pipeline.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire slowapi rate limiting into search endpoint</name>
  <files>
    factfeed/web/main.py
    factfeed/web/routes/search.py
    pyproject.toml
  </files>
  <action>
    1. Install slowapi: run `uv add slowapi`

    2. In `factfeed/web/main.py`:
       - Import `Limiter`, `_rate_limit_exceeded_handler` from `slowapi`, `get_remote_address` from `slowapi.util`, `RateLimitExceeded` from `slowapi.errors`
       - Create the limiter singleton BEFORE the `app` instantiation: `limiter = Limiter(key_func=get_remote_address)`
       - After `app = FastAPI(...)`, add: `app.state.limiter = limiter` and `app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)`

    3. In `factfeed/web/routes/search.py`:
       - Import the limiter: `from factfeed.web.main import limiter`
       - Add `@limiter.limit("30/minute")` decorator to BOTH `search_page` and `search_endpoint` functions
       - CRITICAL: The `@router.get(...)` decorator MUST remain above (before in source) `@limiter.limit(...)` — route decorator outermost, limiter decorator closest to the function definition
       - Both functions already accept `request: Request` as their first parameter, which slowapi requires — no signature change needed

    4. In `pyproject.toml` under `[tool.pytest.ini_options]`:
       - Add `addopts = "-m 'not slow and not uat'"` so the default `pytest` run excludes slow and uat tests automatically
       - Add the `uat` marker to the markers list: `"uat: marks UAT tests that require a seeded database (deselect with '-m not uat')"`
  </action>
  <verify>
    <automated>cd /home/dms/work/claude_data_parser && uv run python -c "from factfeed.web.main import limiter; print('limiter OK')" && uv run python -c "import slowapi; print('slowapi OK')"</automated>
    <manual>Verify that limiter is created and wired in main.py, and that both search routes have the @limiter.limit decorator</manual>
  </verify>
  <done>slowapi installed, limiter singleton created in main.py with exception handler, both search routes decorated with 30/minute limit, pyproject.toml updated with addopts and uat marker</done>
</task>

<task type="auto">
  <name>Task 2: Add rate limit tests and accuracy report artifact</name>
  <files>
    tests/test_rate_limit.py
    tests/nlp/test_pipeline.py
    reports/.gitkeep
  </files>
  <action>
    1. Create `reports/.gitkeep` (empty file) to establish the report output directory.

    2. Create `tests/test_rate_limit.py` with rate limit behavior tests:
       - Import AsyncClient, ASGITransport from httpx, and the standard test fixtures
       - Create a fixture that provides an httpx test client similar to `tests/test_web_routes.py` (override `get_db`, use ASGITransport)
       - IMPORTANT: To avoid rate limit state leaking between tests, create a fresh `Limiter` instance per test by replacing `app.state.limiter` in the fixture. The fresh limiter should use `get_remote_address` as key_func, same as production.
       - Test `test_search_under_rate_limit`: Send 5 requests to `/search?q=test` — all should return 200.
       - Test `test_search_rate_limit_429`: Send 31 rapid requests to `/search?q=test` — the 31st should return 429. Use the `X-Forwarded-For` header set to a unique IP to ensure isolation, or rely on the fresh limiter fixture.
       - Test `test_article_detail_not_rate_limited`: Send 35+ requests to `/article/{id}` — none should return 429 (rate limiting is search-only per user decision).

    3. Extend `test_evaluation_set_accuracy` in `tests/nlp/test_pipeline.py`:
       - After the existing `print()` diagnostic block (before the final `assert`), add code to save a JSON report artifact:
         - Build the report dict with keys: `overall_accuracy` (rounded to 4 decimal places), `total`, `correct`, `threshold` (0.80), `passed` (bool), `per_category` (dict of {category: {correct, total, accuracy}}), `per_label` (dict of {label: {correct, total, accuracy}})
         - Write to `reports/accuracy_report.json` using `Path(__file__).resolve().parents[2] / "reports" / "accuracy_report.json"`
         - Create parent directory with `mkdir(parents=True, exist_ok=True)`
         - Print the report path to stdout for visibility
       - The existing assertion `assert overall_accuracy >= 0.80` MUST remain unchanged after the report write
  </action>
  <verify>
    <automated>cd /home/dms/work/claude_data_parser && uv run pytest tests/test_rate_limit.py -x -v 2>&1 | tail -20</automated>
    <manual>Check that reports/.gitkeep exists and that test_pipeline.py contains the JSON report save logic</manual>
  </verify>
  <done>Rate limit tests pass (200 under limit, 429 on excess, article detail unaffected); accuracy test saves JSON report to reports/accuracy_report.json with pass/fail, per-category, and per-label breakdowns; reports/ directory tracked in git</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_rate_limit.py -x -v` — all rate limit tests pass
2. `uv run python -c "from factfeed.web.main import limiter; print(type(limiter))"` — prints slowapi Limiter class
3. `grep -n "limiter.limit" factfeed/web/routes/search.py` — shows limit decorators on both routes
4. `grep "addopts" pyproject.toml` — shows "-m 'not slow and not uat'"
5. `grep "accuracy_report.json" tests/nlp/test_pipeline.py` — report artifact logic present
6. `ls reports/.gitkeep` — directory exists
</verification>

<success_criteria>
- slowapi rate limiting returns 429 after 30 requests/minute on search endpoints
- Article detail endpoint is NOT rate limited
- Accuracy test saves a machine-readable JSON report to `reports/accuracy_report.json`
- Default `pytest` run excludes slow and uat marked tests
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-hardening/05-01-SUMMARY.md`
</output>
