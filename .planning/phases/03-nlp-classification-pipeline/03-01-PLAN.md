---
phase: 03-nlp-classification-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - Dockerfile
  - alembic/versions/0003_sentence_position_unique.py
  - tests/nlp/__init__.py
  - tests/nlp/eval_dataset.py
  - tests/nlp/conftest.py
autonomous: true
requirements:
  - NLP-05

must_haves:
  truths:
    - "NLP dependencies (spacy, transformers, torch-cpu, scikit-learn) are installed and importable"
    - "spaCy en_core_web_sm model is downloadable in both local and Docker environments"
    - "Sentence table has UniqueConstraint on (article_id, position) enforced at DB level"
    - "A labeled evaluation dataset of 120+ sentences exists for accuracy measurement"
  artifacts:
    - path: "pyproject.toml"
      provides: "NLP dependency declarations"
      contains: "spacy"
    - path: "Dockerfile"
      provides: "spaCy model download in container build"
      contains: "spacy download en_core_web_sm"
    - path: "alembic/versions/0003_sentence_position_unique.py"
      provides: "DB migration adding unique constraint"
      contains: "UniqueConstraint"
    - path: "tests/nlp/eval_dataset.py"
      provides: "120+ labeled sentences for accuracy gate"
      contains: "EVAL_SENTENCES"
    - path: "tests/nlp/conftest.py"
      provides: "Shared NLP test fixtures (mock pipeline, spaCy nlp)"
  key_links:
    - from: "pyproject.toml"
      to: "Dockerfile"
      via: "uv sync installs NLP deps in container"
      pattern: "uv sync"
    - from: "alembic/versions/0003_sentence_position_unique.py"
      to: "factfeed/db/models.py"
      via: "Migration matches Sentence model constraint"
      pattern: "article_id.*position"
---

<objective>
Install all NLP dependencies, create the evaluation dataset, add the sentence position unique constraint migration, and set up test infrastructure for the NLP module.

Purpose: Every subsequent plan depends on NLP libraries being available and the evaluation dataset existing for accuracy measurement. The unique constraint prevents duplicate sentence rows on re-classification.
Output: pyproject.toml with NLP deps, Dockerfile with spaCy model, migration 0003, eval dataset, NLP test conftest.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-nlp-classification-pipeline/03-RESEARCH.md
@factfeed/db/models.py
@pyproject.toml
@Dockerfile
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install NLP dependencies and update Dockerfile</name>
  <files>pyproject.toml, Dockerfile</files>
  <action>
Add the following to `pyproject.toml` dependencies list:
- `"spacy>=3.8.11"`
- `"transformers>=5.2.0"`
- `"torch>=2.10.0"`
- `"scikit-learn>=1.8.0"`

Also add `"scipy>=1.12.0"` (needed for temperature scaling `minimize_scalar`).

Run `uv lock` to regenerate the lock file after editing pyproject.toml. Then run `uv sync` to install locally.

Update the Dockerfile to download the spaCy English model after dependency install:
```dockerfile
RUN uv sync --frozen --no-dev
RUN uv run python -m spacy download en_core_web_sm
```

The `RUN uv run python -m spacy download en_core_web_sm` line must come AFTER `uv sync` but BEFORE the COPY of application code (to maximize Docker layer caching — model download is slow and does not change with code edits).

Verify: `uv run python -c "import spacy; import transformers; import torch; import sklearn; print('OK')"` prints OK.
  </action>
  <verify>
    <automated>cd /home/dms/work/claude_data_parser && uv run python -c "import spacy; import transformers; import torch; import sklearn; print('OK')"</automated>
    <manual>Check pyproject.toml has all 5 NLP deps listed</manual>
  </verify>
  <done>All NLP libraries are importable. Dockerfile includes spaCy model download. uv.lock is regenerated.</done>
</task>

<task type="auto">
  <name>Task 2: Create Alembic migration for sentence unique constraint and evaluation dataset</name>
  <files>alembic/versions/0003_sentence_position_unique.py, tests/nlp/__init__.py, tests/nlp/eval_dataset.py, tests/nlp/conftest.py</files>
  <action>
**Migration (0003):**
Create a hand-written Alembic migration `alembic/versions/0003_sentence_position_unique.py` that:
- Has `revision` as a new unique ID (use a short hex string like previous migrations)
- Has `down_revision` pointing to the 0002 migration revision ID (read it from `0002_article_ingestion_fields.py`)
- Upgrade: `op.create_unique_constraint("uq_sentences_article_position", "sentences", ["article_id", "position"])`
- Downgrade: `op.drop_constraint("uq_sentences_article_position", "sentences", type_="unique")`

Also update the Sentence model in `factfeed/db/models.py` to declare the constraint in `__table_args__`:
```python
from sqlalchemy import UniqueConstraint

class Sentence(Base):
    __tablename__ = "sentences"
    __table_args__ = (
        UniqueConstraint("article_id", "position", name="uq_sentences_article_position"),
    )
    # ... existing columns unchanged
```

**Test infrastructure:**
Create `tests/nlp/__init__.py` (empty file).

Create `tests/nlp/conftest.py` with shared fixtures:
- `mock_zs_pipeline` fixture: returns a callable that mimics HuggingFace zero-shot-classification pipeline output format: `{"labels": [...], "scores": [...], "sequence": text}`. Default behavior: return "factual statement" with score 0.85.
- `spacy_nlp` fixture (session-scoped): loads `spacy.load("en_core_web_sm")` once for all NLP tests.

**Evaluation dataset:**
Create `tests/nlp/eval_dataset.py` with a list `EVAL_SENTENCES` containing 120+ labeled sentence dicts. Each entry: `{"text": "...", "expected_label": "fact"|"opinion"|"mixed"|"unclear", "category": "easy"|"hard"|"edge_case"}`.

Include at minimum:
- 30 clear factual sentences (statistics, dates, named events, scientific measurements)
- 30 clear opinion sentences (should/must judgments, value claims, predictions, editorializing)
- 20 attributed speech sentences ("The minister said...", "According to experts...") — expected: mixed
- 15 short/ambiguous sentences (under 30 tokens, headlines, fragments) — expected: unclear
- 15 hard cases (hedging language, implied opinion in factual framing, statistics with embedded judgment, rhetorical questions)
- 10 edge cases (satire markers, breaking news stubs, single-word sentences)

Source sentences from typical news prose patterns (BBC, Reuters, AP style). Do NOT copy verbatim from copyrighted articles — write original sentences in the same style.

Mark hard cases with `"category": "hard"` so the accuracy test can report per-category breakdowns.
  </action>
  <verify>
    <automated>cd /home/dms/work/claude_data_parser && uv run python -c "from tests.nlp.eval_dataset import EVAL_SENTENCES; assert len(EVAL_SENTENCES) >= 120; labels = set(s['expected_label'] for s in EVAL_SENTENCES); assert labels == {'fact', 'opinion', 'mixed', 'unclear'}; print(f'OK: {len(EVAL_SENTENCES)} sentences, labels: {labels}')"</automated>
    <manual>Spot-check 10 random sentences for correct labeling</manual>
  </verify>
  <done>Migration 0003 adds unique constraint on (article_id, position). Sentence model declares matching __table_args__. Evaluation dataset has 120+ labeled sentences across all 4 label categories. NLP test conftest provides mock pipeline and spaCy fixtures.</done>
</task>

</tasks>

<verification>
- `uv run python -c "import spacy; import transformers; import torch; import sklearn"` succeeds
- `tests/nlp/eval_dataset.py` contains 120+ entries with all 4 labels represented
- `tests/nlp/conftest.py` provides mock_zs_pipeline and spacy_nlp fixtures
- Migration 0003 exists and chains from 0002
- Sentence model has UniqueConstraint in __table_args__
</verification>

<success_criteria>
All NLP dependencies installed and importable. Evaluation dataset with 120+ labeled sentences exists. Alembic migration 0003 ready. NLP test infrastructure (conftest, __init__) in place. Dockerfile updated for spaCy model.
</success_criteria>

<output>
After completion, create `.planning/phases/03-nlp-classification-pipeline/03-01-SUMMARY.md`
</output>
