---
phase: 02-ingestion-pipeline
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - tests/ingestion/__init__.py
  - tests/ingestion/test_deduplicator.py
  - tests/ingestion/test_extractor.py
  - tests/ingestion/test_fetcher.py
  - tests/ingestion/test_runner.py
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
  - INGEST-03
  - INGEST-04

must_haves:
  truths:
    - "Unit tests verify compute_url_hash normalizes URLs and produces consistent 64-char hex digests"
    - "Unit tests verify extract_article falls back to RSS summary when trafilatura returns None or short content"
    - "Unit tests verify fetch_rss_feed handles bozo feeds without crashing"
    - "Integration test verifies run_ingestion_cycle processes entries, deduplicates, and persists articles"
    - "All tests pass without requiring network access or live RSS feeds"
  artifacts:
    - path: "tests/ingestion/__init__.py"
      provides: "Test package marker"
    - path: "tests/ingestion/test_deduplicator.py"
      provides: "Unit tests for URL hash computation and normalization"
      min_lines: 30
    - path: "tests/ingestion/test_extractor.py"
      provides: "Unit tests for article extraction and partial fallback"
      min_lines: 40
    - path: "tests/ingestion/test_fetcher.py"
      provides: "Unit tests for RSS feed fetching and article page fetching with mocked HTTP"
      min_lines: 40
    - path: "tests/ingestion/test_runner.py"
      provides: "Integration test for full ingestion cycle with mocked feeds and DB"
      min_lines: 60
  key_links:
    - from: "tests/ingestion/test_runner.py"
      to: "factfeed/ingestion/runner.py"
      via: "Tests run_ingestion_cycle with mocked dependencies"
      pattern: "run_ingestion_cycle"
    - from: "tests/ingestion/test_deduplicator.py"
      to: "factfeed/ingestion/deduplicator.py"
      via: "Tests compute_url_hash determinism and normalization"
      pattern: "compute_url_hash"
---

<objective>
Create comprehensive unit and integration tests for the ingestion pipeline modules using mocked HTTP and database fixtures.

Purpose: The ingestion pipeline touches external services (RSS feeds, article pages, PostgreSQL). Tests must verify behavior without network access using mocked httpx responses, mocked feedparser output, and the existing test database fixtures. This validates all five phase requirements (INGEST-01 through INGEST-04, INFRA-03) in an automated, repeatable way.
Output: Test suite covering deduplicator, extractor, fetcher, and runner with all tests passing
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ingestion-pipeline/02-03-SUMMARY.md
@factfeed/ingestion/deduplicator.py
@factfeed/ingestion/extractor.py
@factfeed/ingestion/fetcher.py
@factfeed/ingestion/runner.py
@factfeed/ingestion/persister.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests for deduplicator and extractor</name>
  <files>tests/ingestion/__init__.py, tests/ingestion/test_deduplicator.py, tests/ingestion/test_extractor.py</files>
  <action>
Create `tests/ingestion/__init__.py` as an empty package marker.

**Create `tests/ingestion/test_deduplicator.py`** with these test cases:

1. `test_compute_url_hash_returns_64_char_hex` — basic hash output format
2. `test_compute_url_hash_deterministic` — same URL produces same hash on repeated calls
3. `test_compute_url_hash_strips_query_params` — `https://example.com/article?utm_source=twitter` and `https://example.com/article` produce the same hash
4. `test_compute_url_hash_strips_fragment` — `https://example.com/article#section1` and `https://example.com/article` produce the same hash
5. `test_compute_url_hash_normalizes_case` — `HTTPS://Example.COM/article` and `https://example.com/article` produce the same hash
6. `test_compute_url_hash_different_paths_different_hash` — `https://example.com/a` and `https://example.com/b` produce different hashes
7. `test_article_exists_returns_false_for_nonexistent` — with empty DB, article_exists returns False (needs db_session fixture)
8. `test_article_exists_returns_true_after_insert` — insert an article, article_exists returns True (needs db_session fixture)

Tests 7 and 8 are async and use the `db_session` fixture from conftest.py. They need to insert via raw SQL or the ORM model to set up state.

**Create `tests/ingestion/test_extractor.py`** with these test cases:

1. `test_extract_article_returns_partial_on_none_html` — passing None-equivalent content triggers partial fallback with rss_summary as body
2. `test_extract_article_returns_partial_on_short_content` — mock `trafilatura.bare_extraction` to return `{"text": "short"}` (< 200 chars), verify is_partial=True and body=rss_summary
3. `test_extract_article_returns_full_on_good_content` — mock `trafilatura.bare_extraction` to return `{"text": "A" * 300, "author": "Test Author", "date": "2025-01-15", "image": "https://example.com/img.jpg"}`, verify is_partial=False and all metadata populated
4. `test_extract_article_returns_partial_on_exception` — mock `trafilatura.bare_extraction` to raise RuntimeError, verify partial fallback
5. `test_parse_article_date_returns_datetime_for_iso` — "2025-01-15T10:30:00Z" returns correct UTC datetime
6. `test_parse_article_date_returns_none_for_none` — None input returns None
7. `test_parse_article_date_returns_none_for_garbage` — unparseable string returns None

Use `unittest.mock.patch` to mock trafilatura functions. These are pure unit tests — no DB or network needed.
  </action>
  <verify>
    <automated>cd /home/dms/work/claude_data_parser && uv run pytest tests/ingestion/test_deduplicator.py tests/ingestion/test_extractor.py -v --ignore=tests/db/ -k "not article_exists" --no-header -q 2>&1 | tail -5</automated>
    <manual>Verify all non-DB tests pass. DB tests (test_article_exists_*) require a running PostgreSQL and are excluded from this quick check.</manual>
  </verify>
  <done>test_deduplicator.py has 8 tests covering hash format, determinism, URL normalization (case, query, fragment), and DB existence check. test_extractor.py has 7 tests covering full extraction, partial fallback (None/short/exception), and date parsing. All non-DB tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for fetcher and integration test for runner</name>
  <files>tests/ingestion/test_fetcher.py, tests/ingestion/test_runner.py</files>
  <action>
**Create `tests/ingestion/test_fetcher.py`** with mocked httpx tests:

1. `test_fetch_rss_feed_success` — mock `httpx.AsyncClient.get` to return a Response with valid RSS XML bytes, verify feedparser dict returned with entries
2. `test_fetch_rss_feed_bozo_continues` — mock response with malformed XML that causes feedparser bozo=True, verify function returns the feed (not raises) and entries are still accessible
3. `test_fetch_rss_feed_http_error_raises` — mock response with 500 status, verify httpx.HTTPStatusError is raised
4. `test_fetch_article_page_success` — mock response with HTML bytes, verify bytes returned
5. `test_fetch_article_page_timeout_returns_none` — mock httpx.TimeoutException, verify None returned
6. `test_fetch_article_page_http_error_returns_none` — mock 404 response, verify None returned

Use `unittest.mock.AsyncMock` for the httpx client. Create a minimal valid RSS XML string for feed tests:
```python
SAMPLE_RSS = b"""<?xml version="1.0"?>
<rss version="2.0">
<channel><title>Test</title>
<item><title>Article 1</title><link>https://example.com/1</link><description>Summary</description></item>
</channel></rss>"""
```

**Create `tests/ingestion/test_runner.py`** with integration-style tests using mocked dependencies:

1. `test_run_ingestion_cycle_processes_entries` — mock all external calls:
   - Mock `fetch_rss_feed` to return a feedparser dict with 2 entries
   - Mock `article_exists` to return False (both are new)
   - Mock `can_fetch` to return True
   - Mock `fetch_article_page` to return sample HTML bytes
   - Mock `extract_article` to return a full extraction dict
   - Mock `save_article` to return True
   - Verify: save_article called twice (once per entry)

2. `test_run_ingestion_cycle_skips_duplicates` — same as above but `article_exists` returns True for first entry:
   - Verify: save_article called once (only the non-duplicate)

3. `test_run_ingestion_cycle_continues_after_source_error` — mock `fetch_rss_feed` to raise Exception for first source, return valid feed for second:
   - Verify: second source still processed, no unhandled exception

4. `test_run_ingestion_cycle_handles_partial_extraction` — mock `fetch_article_page` to return None for an entry:
   - Verify: article still saved with is_partial=True (fallback to RSS summary)

Use `unittest.mock.patch` and `AsyncMock` extensively. Mock at the module boundary (e.g., `patch("factfeed.ingestion.runner.fetch_rss_feed")`). These tests do NOT require a running database — mock the session_factory and session.

Install `pytest-mock` if not already available, or use stdlib `unittest.mock`.
  </action>
  <verify>
    <automated>cd /home/dms/work/claude_data_parser && uv run pytest tests/ingestion/test_fetcher.py tests/ingestion/test_runner.py -v --no-header -q 2>&1 | tail -10</automated>
  </verify>
  <done>test_fetcher.py has 6 tests covering successful feed/article fetch, bozo handling, and HTTP/timeout error handling. test_runner.py has 4 integration tests covering normal processing, duplicate skipping, source error resilience, and partial extraction fallback. All tests pass without network or database access.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/ingestion/ -v --no-header -q -k "not article_exists"` — all non-DB tests pass
2. All test files import without errors
3. Tests use mocks exclusively — no network calls, no live RSS feeds
4. Test coverage: deduplicator (hash logic), extractor (extraction + fallback), fetcher (HTTP success/failure), runner (orchestration + error handling)
</verification>

<success_criteria>
- tests/ingestion/ package with __init__.py and 4 test files
- test_deduplicator.py: 8 tests (hash format, determinism, normalization, DB check)
- test_extractor.py: 7 tests (full extraction, partial fallback variations, date parsing)
- test_fetcher.py: 6 tests (feed fetch, article fetch, error handling)
- test_runner.py: 4 tests (full cycle, dedup skip, source error resilience, partial extraction)
- Total: 25 tests, all passing without network or live database
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingestion-pipeline/02-04-SUMMARY.md`
</output>
