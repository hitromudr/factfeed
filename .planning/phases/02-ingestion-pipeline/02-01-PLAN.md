---
phase: 02-ingestion-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - factfeed/db/models.py
  - factfeed/config.py
  - alembic/versions/0002_article_ingestion_fields.py
  - pyproject.toml
autonomous: true
requirements:
  - INGEST-04
  - INFRA-03

must_haves:
  truths:
    - "Article model has is_partial, author, lead_image_url, and body_html columns available for ingestion code to populate"
    - "Settings object exposes ingest_interval_minutes configurable via INGEST_INTERVAL_MINUTES env var with default of 15"
    - "Alembic migration 0002 adds the new columns to articles table and runs cleanly after 0001"
    - "All new dependencies (feedparser, httpx, trafilatura, apscheduler, structlog, ftfy) are installable via uv"
  artifacts:
    - path: "alembic/versions/0002_article_ingestion_fields.py"
      provides: "Migration adding is_partial, author, lead_image_url, body_html columns to articles"
      contains: "op.add_column"
    - path: "factfeed/db/models.py"
      provides: "Updated Article model with ingestion columns"
      contains: "is_partial"
    - path: "factfeed/config.py"
      provides: "Settings with ingest_interval_minutes"
      contains: "ingest_interval_minutes"
    - path: "pyproject.toml"
      provides: "All Phase 2 dependencies declared"
      contains: "trafilatura"
  key_links:
    - from: "alembic/versions/0002_article_ingestion_fields.py"
      to: "factfeed/db/models.py"
      via: "Column names match between migration and ORM model"
      pattern: "is_partial.*author.*lead_image_url.*body_html"
---

<objective>
Add the database columns, configuration, and dependencies needed by the ingestion pipeline before any ingestion code is written.

Purpose: Phase 1 built a minimal schema. Phase 2 requires `is_partial` (partial extraction flag), `author`, `lead_image_url`, and `body_html` (minimal HTML for display) columns on the Article model. The config needs `ingest_interval_minutes` for APScheduler. All new libraries must be installed.
Output: Updated Article model, Alembic migration 0002, expanded Settings, all deps in pyproject.toml
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@factfeed/db/models.py
@factfeed/config.py
@alembic/versions/0001_initial_schema.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 2 dependencies and update config</name>
  <files>pyproject.toml, factfeed/config.py</files>
  <action>
Install all Phase 2 dependencies using uv:
```bash
uv add feedparser httpx trafilatura apscheduler structlog ftfy
```

Update `factfeed/config.py` to add ingestion-related settings to the `Settings` class:
- `ingest_interval_minutes: int = 15` — configurable poll interval, read from `INGEST_INTERVAL_MINUTES` env var
- `user_agent: str = "FactFeed/1.0 RSS reader"` — shared User-Agent string for all HTTP requests
- `article_fetch_delay: float = 1.5` — delay in seconds between sequential article page fetches within a source (politeness)
- `consecutive_failure_threshold: int = 3` — after N consecutive failures per source, escalate log level to ERROR

These are all Claude's Discretion items from CONTEXT.md. The env var names follow pydantic-settings convention (uppercased field name).
  </action>
  <verify>
    <automated>cd /home/dms/work/claude_data_parser && uv run python -c "from factfeed.config import settings; assert settings.ingest_interval_minutes == 15; assert settings.user_agent == 'FactFeed/1.0 RSS reader'; print('config OK')" && uv run python -c "import feedparser, httpx, trafilatura, apscheduler, structlog, ftfy; print('imports OK')"</automated>
  </verify>
  <done>All six new libraries import successfully. Settings exposes ingest_interval_minutes (default 15), user_agent, article_fetch_delay, and consecutive_failure_threshold with sensible defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Add ingestion columns to Article model and create Alembic migration</name>
  <files>factfeed/db/models.py, alembic/versions/0002_article_ingestion_fields.py</files>
  <action>
Update `factfeed/db/models.py` — add four new columns to the `Article` class:
- `author = Column(Text, nullable=True)` — article author name from extraction metadata
- `lead_image_url = Column(Text, nullable=True)` — lead image URL from extraction metadata
- `body_html = Column(Text, nullable=True)` — minimal HTML body (paragraphs + blockquotes preserved) for Phase 4 display
- `is_partial = Column(Boolean, nullable=False, server_default="false")` — True when extraction failed and RSS summary was used as fallback

Import `Boolean` from sqlalchemy at the top of the file. Place new columns after existing `body` column, before `published_at`.

Create `alembic/versions/0002_article_ingestion_fields.py` as a hand-written migration:
- revision: generate a new revision ID (use `alembic revision -m "add article ingestion fields"` to get the file, then edit)
- down_revision: must point to the 0001 initial schema revision ID
- `upgrade()`: four `op.add_column("articles", ...)` calls matching the model columns exactly
- `downgrade()`: four `op.drop_column("articles", ...)` calls in reverse

The `is_partial` column must use `server_default=sa.text("false")` so existing rows get `false` without a full table rewrite.

Do NOT use `alembic revision --autogenerate` — autogenerate may produce incorrect output due to the GENERATED tsvector column. Write the migration by hand following the pattern from 0001.
  </action>
  <verify>
    <automated>cd /home/dms/work/claude_data_parser && uv run python -c "from factfeed.db.models import Article; cols = [c.name for c in Article.__table__.columns]; assert 'is_partial' in cols; assert 'author' in cols; assert 'lead_image_url' in cols; assert 'body_html' in cols; print('model OK')" && uv run python -c "import importlib, pathlib; versions = list(pathlib.Path('alembic/versions').glob('0002_*.py')); assert len(versions) == 1; print(f'migration exists: {versions[0].name}')"</automated>
  </verify>
  <done>Article model has is_partial (Boolean, server_default false), author (Text), lead_image_url (Text), body_html (Text) columns. Migration 0002 exists with matching add_column and drop_column operations. Migration chain is 0001 -> 0002.</done>
</task>

</tasks>

<verification>
1. `uv run python -c "from factfeed.config import settings; print(settings.ingest_interval_minutes)"` prints 15
2. `uv run python -c "import feedparser, httpx, trafilatura, apscheduler, structlog, ftfy"` exits 0
3. `uv run python -c "from factfeed.db.models import Article; print([c.name for c in Article.__table__.columns])"` includes is_partial, author, lead_image_url, body_html
4. Alembic migration 0002 file exists with correct down_revision pointing to 0001
</verification>

<success_criteria>
- All six Phase 2 dependencies installed and importable
- Settings class has ingest_interval_minutes, user_agent, article_fetch_delay, consecutive_failure_threshold
- Article model has four new columns (is_partial, author, lead_image_url, body_html)
- Alembic migration 0002 correctly chains after 0001
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingestion-pipeline/02-01-SUMMARY.md`
</output>
